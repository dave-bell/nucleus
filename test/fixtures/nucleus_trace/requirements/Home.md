# Home (fixture)

Mirrors the real `docs/requirements/Home.md`: a worked example of action
formatting that happens to match the `### PREFIX-A##` header shape, and must
be excluded from the defined set by filename, not by content.

### FIX-A99 — Worked example only, not a real fixture requirement
